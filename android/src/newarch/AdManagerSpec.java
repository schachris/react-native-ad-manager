package com.admanager;

import com.facebook.react.bridge.ReactApplicationContext;

abstract class AdManagerSpec extends NativeAdManagerSpec {
  AdManagerSpec(ReactApplicationContext context) {
    super(context);
  }
}
